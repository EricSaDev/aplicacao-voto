apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: vote
spec:
  hosts:
  - "*"
  gateways:
  - vote-gateway
  http:
  - match:
    - uri:
        exact: /vote
    route:
    - destination:
        host: vote
        port:
          number: 80
